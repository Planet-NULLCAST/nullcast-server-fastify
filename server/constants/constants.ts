import { TagsVisibility, TagsStatus } from 'interfaces/tags.type';

export const TAGS_VISIBILITY: TagsVisibility[] = ['public', 'private'];
export const TAGS_STATUS: TagsStatus[] = ['active', 'inactive'];
